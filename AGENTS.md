## Code Style

Use elvis ?: operator instead of ternary in tsx.
```tsx
{condition
    ? <span>True</span>
    : <span>False</span>
}
```

## Tools and Libraries

Use preact instead of React.
Use yarn not npm or pnpm.
